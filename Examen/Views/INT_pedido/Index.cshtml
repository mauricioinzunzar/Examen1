@model IEnumerable<Examen.Models.INT_pedido>

@{
    ViewBag.Title = "Clientes";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nuevo", "Crear")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.INT_Clientes.cliNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.INT_Venta.ventNombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.INT_Clientes.cliNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.INT_Venta.ventNombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.pedidoId }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.pedidoId }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.pedidoId })
        </td>
    </tr>
}

</table>
